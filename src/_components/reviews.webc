<script webc:setup>
	// Reviews data is now loaded from src/_data/reviews.json
</script>

<section id="reviews" class="reviews-section">
	<h3>Reviews</h3>
	<figure class="review-card" webc:for="review of $data.reviews">
		<blockquote @text="review.quote" class="quote"></blockquote>
		<figcaption @raw="review.source" class="reviewer"></figcaption>
	</figure>
</section>

<style>
	#reviews {
		text-align: center;

		h3 {
			font-family: var(--font-cursive);
			font-size: var(--step-4);
			padding-block: var(--space-s);
		}

		.review-card {
			max-width: 700px;
			margin: 0 auto 2.5rem auto;
			padding: 1.5rem 2rem;
			border: 1px solid var(--light-beige);
			box-shadow: 3px 3px 10px var(--shadow-light);
			position: relative;
			font-size: 1.2rem;
		}

		.quote {
			font-family: var(--font-italic);
			font-style: italic;
			line-height: 1.6;
			position: relative;
			text-align: justify;
		}

		.quote::before,
		.quote::after {
			content: "“";
			font-family: serif;
			font-size: 2.5rem;
			position: absolute;
			opacity: 0.3;
		}

		.quote::before {
			left: -1.5rem;
			top: -1.2rem;
		}

		.quote::after {
			content: "”";
			transform: translateY(-16px);
		}

		.reviewer {
			margin-top: 1rem;
			text-align: right;
			font-style: normal;
			font-size: 1rem;
		}
	}
</style>
