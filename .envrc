use flake .

layout node

# Exports .env into the SHELL, which is what pnpm needs: .npmrc expands
# ${GITHUB_PACKAGES_TOKEN} at install time, and the dotenv.config() in the
# site's own code only reaches Node at runtime — far too late for an install.
dotenv_if_exists .env

